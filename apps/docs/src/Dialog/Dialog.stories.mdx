import { ArgsTable, Title,  Controls, Meta, Story, Canvas } from '@storybook/addon-docs';
import { AlertDialog } from '@custompackages/designsystem';
import AlertDialogExample from './AlertDialogExample'

<Meta title="Example/AlertDialog"
      component={AlertDialog}
      tags={['autodocs']} 
/>

<Story name="BasicDialog" args={{ header: 'test' }}/>

<Title />

## 설치

프로젝트에 @custompackages/designsystem이 설치되어 있는지 확인하세요. 

다음과 같이 AlertDialog 컴포넌트를 임포트할 수 있습니다

```jsx
import { AlertDialog } from '@custompackages/designsystem';
```

## AlertDialogExample 

<AlertDialogExample />
 

 코드 예시

 ```jsx

 const AlertDialogExample  = () => {
  const [isVisible, setVisible] = useState(false)

  return (
    <>
      <button
        type="button"
        onClick={() => {
          setVisible(true)
        }}
      >
        click button !
      </button>
      <AlertDialog isVisible={isVisible} onChangeVisible={setVisible} header="example" onSubmit={async (e) => {}}>
        <AlertDialog.Body>Test Body</AlertDialog.Body>
      </AlertDialog>
    </>
  )
}
 
 ```

## Props

<Controls />

 